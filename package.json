{
  "scripts": {
    "frontend": "cd frontend && yarn && yarn dev",
    "backend": "cd backend && yarn && yarn server",
    "database": "cd backend && yarn && yarn db",
    "dev": "yarn database & yarn backend & yarn frontend",
    "clear": "rm -rf backend/node_modules frontend/node_modules",
    "gs": "cd backend && yarn secret"
  }
}